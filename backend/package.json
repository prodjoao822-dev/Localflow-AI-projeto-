{
  "name": "localflow-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LocalFlow AI — backend monólito modular (Node.js + Express + Prisma/PostgreSQL + Redis/BullMQ)",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@prisma/client": "^5.19.0",
    "bullmq": "^5.12.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pino": "^9.3.2",
    "pino-pretty": "^11.2.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.19.0"
  }
}
